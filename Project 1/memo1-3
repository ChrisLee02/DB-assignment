1. where절 처리

1-1. table.column을 고려할 수 있는가?
- row에 table.column을 저장하도록 한다.
- column만 주어진 경우에는 from절에서 주어진 테이블을 이용한다.
- 그냥 씨발 condition 객체를 반환하도록 구현할게요 ㅋ
- resolve 함수를 database에서 구현해두면, 메타데이터 접근도 쌉가능~


- 연산의 가능 여부도 커스터마이징이 필요함. 이것도 메타데이터를 받아와야 가능함.
-> 이거 저장시에 문자열에 ""를 주면 되는거 아닐까?
결국 핵심은 date이랑 str을 분리하는거임.


2. select 처리
- 이 씨발 lark부터 고쳐놔야함.